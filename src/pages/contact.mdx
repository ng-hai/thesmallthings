import Post from "components/blog/post"
import dynamic from "next/dynamic"

export const ContactForm = dynamic(() => import("components/contact-form"), {
  ssr: false,
})

export const meta = {
  title: `Contact — The Small Things`,
}

export default Post(meta)

# Contact

Your messages will be sent to my Telegram account. Want to know how it works? [Check this note out](/blog/the-small-bot).

If you prefer the formal way, please send email to hello@ng-hai.dev.

<ContactForm className="mt-4" style={{ maxWidth: 480 }} />
